info:
  name: tarifa
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/tarifas/
  body:
    type: json
    data: |-
      {
        "nombre": "Promo 1 Hora",
        "precio_por_hora": "1500.00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
